const { makeStyles } = require("@material-ui/styles")

export const useStyles = makeStyles({
    doctorsContainer: {
        // border:"1px solid red",
        height: "100%",
        padding: "20px"
    }
});